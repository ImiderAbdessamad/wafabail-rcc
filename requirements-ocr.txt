# Optional local OCR/document-parser stack tested by the RCC hybrid pipeline.
# Install after the core requirements:
#   python -m pip install -r requirements.txt -r requirements-ocr.txt
rapidocr==3.9.2
onnxruntime==1.28.0
docling==2.119.0
